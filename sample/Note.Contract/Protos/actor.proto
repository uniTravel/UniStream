syntax = "proto3";

option csharp_namespace = "Note.Contract";

package actor;

service Actor {
    rpc CreateActorCommand (CreateActor) returns (CreateActorReply);
}

message CreateActor {
    string name = 1;
}

message CreateActorReply {
    string agg_id = 1;
    string trace_id = 2;
}
